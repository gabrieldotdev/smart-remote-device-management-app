import 'package:flutter/material.dart';

class ExploreScreenPage extends StatelessWidget {
  const ExploreScreenPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Explore Screen"),
      ),
    );
  }
}
